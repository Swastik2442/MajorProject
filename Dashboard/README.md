# NMS Dashboard

## Setup

1. `npm i`
2. Set the environment variables in a `.env` file as mentioned in [`.env.sample`](./.env.sample).
3. `npm run dev`
4. Visit [`localhost:5173`](http://localhost:5173)
