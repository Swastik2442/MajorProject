[project]
name = "api"
version = "0.4.0"
description = "API Service to serve NMS Alerts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common",
    "fastapi>=0.121.0",
    "pydantic>=2.12.3",
    "pydantic-settings>=2.11.0",
]

[tool.uv.sources]
common = { path = "../common" }
